name: Run E2E Dev Fakestore Every Thursday 2am IST
on:
  schedule:
    # 2:00am IST is 8:30pm UTC (previous day, IST is UTC+5:30)
    - cron: '30 20 * * 3'
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run All Tests
        run: npx playwright test tests/bookingApp --project=chromium
      - name: Debug workspace
        run: ls -R
      - name: Check if playwright-report exists
        run: if [ -d "reports/playwright-report" ]; then echo "Report folder exists"; else echo "No report generated"; exit 1; fi
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: reports/playwright-report/
          retention-days: 30